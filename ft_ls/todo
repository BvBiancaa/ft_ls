Exit status:
 0  if OK,
 1  if minor problems (e.g., cannot access subdirectory),
 2  if serious trouble (e.g., cannot access command-line argument).
 //add exit status flag to struct senno' la sovrascrive

 acl e extended attributes 
 incolonnamento ls / -l
 ordinare codice
controlla order dirs con -R
gli sfondi dei colori
executable first for ls but second for ./ft_ls